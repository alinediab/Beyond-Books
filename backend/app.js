// This file appears to be unused - server.js is the main entry point
// Keeping for reference but routes are configured in server.js
const routes = require('./routes/index.js');
// app.use('/api', routes); // all routes under /api prefix
